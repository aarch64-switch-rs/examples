[package]
name = "gpu-simple"
version = "0.1.0"
authors = ["XorTroll"]
edition = "2021"

[dependencies]
nx = { workspace = true , features = [ "input", "canvas", "truetype" ] }

[package.metadata.nx.nro]
nacp = { default_name = "gpu-simple", default_author = "XorTroll", version = "Example" }
